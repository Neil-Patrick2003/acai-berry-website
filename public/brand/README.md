# Brand marks

`logo-glow.png` is the "açaí berry GLOW" lockup used in the footer, cropped from
the design reference at 250×150 and upscaled 2×. Replace it with the vector
original — an SVG is preferable here, in which case update the `<Image>` in
`components/site-footer.tsx` accordingly.
