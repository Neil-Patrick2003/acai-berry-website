# Product photography — placeholders

`starter-ritual.png`, `two-pouch-bundle.png` and `three-pouch-bundle.png` are
**temporary** crops from the design reference screenshot (≈190px wide at source,
upscaled 3×). Replace each with the real pack shot at ≥1200px wide, keeping the
file names so `components/product-collection.tsx` needs no edit.

The cards render them at `aspect-7/6` with `object-cover`, so shoot or crop to
roughly that ratio to avoid unexpected cropping.
